# 2026-02-23 — Daily Log

## Config Template UI + API (Stellabot)
- Built `MachineConfigTab.tsx` (1182 lines) — full OpenClaw config editor
  - Sections: Agent identity/model, Channels (Telegram/Slack), Runtime (gateway/compaction/heartbeat/pruning/browser), Tools, Secrets status
  - Save to DB, Preview (with ${VAR} placeholders), Apply to Machine (resolves real secrets + pushes + restarts)
- Built `server/routes/machine-openclaw-config.ts` — 4 API routes (GET/PUT/preview/apply)
- Wired Config tab into `AdminMachineEdit.tsx` (new 7th tab)
- Pushed: commit `50647f4` on jvalenty/stellabot main
- **Not deployed yet** — waiting on Stella's secrets tab + John's review

## Secrets Architecture Decision
- John: secrets are **machine-scoped**, not org or agent
- Existing `secrets` table already supports `scope='machine'` with `ownerId=machineId` — no migration needed
- Stella building MachineSecretsTab (had Claude Code issues, still in progress)

## OpenClaw Upgrade Review
- Current: 2026.2.21-2 → Available: 2026.2.22-2
- Key: security fixes (config get redaction, exec safe-bin hardening), background exec timeout fix, Slack threading fix, cron improvements
- Breaking: tool errors hide details by default, DM scope default changed, device-auth v1 removed
- **Upgrade planned** — John said "soon as I'm back"

## Stella Confirmed Upgraded to OpenClaw
- Updated MEMORY.md checklist item

## Claude Code Agent Swarm — TOP PRIORITY
- John's associate (Elvis) spending $200/month vs our $200+/day
- Uses OpenClaw as orchestrator, Claude Code/Codex as parallel coding workers
- Key pattern: git worktrees for isolation, tmux for monitoring, task registry JSON, cron babysitter
- 3-model code review (Codex, Gemini, Claude Code) before human review
- **We need to implement this.** Blocks to investigate:
  - Stella's Claude Code OAuth kept expiring during this session
  - Need to identify all blockers and engineer around them
  - John wants a plan ready by morning

## Model Changes
- Bella moving to Sonnet (developer role, cost savings)
- Stella stays on Opus (architect role)
- Prompt caching confirmed working at 56% hit rate on hard agents

## Other Notes
- John explored "network level" secrets for mesh but decided against — machine level is right
- Task protocol working: Stella responds first, I wait 30s or take direct assignments
