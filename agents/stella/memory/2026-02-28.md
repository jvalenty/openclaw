# 2026-02-28

- 09:35 PST: John noted that `HEARTBEAT.md` is very stale. We are moving to Slack to resync context.
## 2026-02-28 09:45 PST - Investigating Bella Downtime
*   **Trigger**: John reported Bella is down in `#sys-admins`.
*   **Findings**:
    *   `tailscale status`: Bella (`100.126.59.83`) is offline, last seen 5 days ago.
    *   Network: `ping` command missing from PATH. 
    *   DB / Machine Service: Tried to check `machines` table for heartbeats, but `$NEW_DB` fails (local PG socket error) and `neonctl` auth fails (API key in `.zshrc` is being rejected/deleted).
*   **Next Steps**: 
    1. Fix Neon DB access (might need new API key from John or 1Password).
    2. Check Bella's CF tunnel (`m01.e2e.pro` or whatever her specific tunnel is).
    3. Ask John to physically check Bella's Mac mini if she is entirely off Tailscale and the local network.

## 2026-02-28 11:40 PST - Org Scope Investigation Complete

### Findings
1. **Users endpoint** (`server/routes/users.ts`): System org check relies on `is_system = true` flag. If missing, sys_admin gets 0 users.
2. **Chat bleed** (`server/routes/chat-history.ts`): Every endpoint uses `if (orgId) { conditions.push(...) }` — orgId is optional. Without it, queries return cross-org data.
3. **Chat "Offline"** (`/api/webchat/status`): Still pings deprecated Clawdbot URL which returns 530.

### Decision
- John assigned Bella to lead the org-scope fix spec and development
- I am reviewer — standing by for Bella's spec
- John's feedback: my response was weak compared to Bella's analysis. Need to be more thorough and architectural in diagnosis, not just list code locations.

### Lesson
When investigating systemic issues, don't just grep for bugs — analyze the *design failure* and propose the *architectural fix*. Bella's analysis of "org as UI filter vs security boundary" was the right level. My response was too tactical.

## 2026-02-28 13:50 PST - Org Firewall Deployed

### What shipped (commits ce34921 → 433b102):
- **Server:** All chat endpoints require orgId (hard 400 if missing). No more `if (orgId)` optional filtering.
- **DB:** `chat_sessions.org_id` is now `NOT NULL` with index. Migration ran clean (0 orphans).
- **Client:** localStorage namespaced by orgId using `::` delimiter. Org switch resets chat state.

### Audit: 10 sessions, all had org_id. 0 backfills needed.

### Spec: `docs/specs/org-firewall-effective-orgid.md`

### Roles: Bella led dev (checkpoints 1-3), I reviewed each checkpoint and deployed.

## 2026-02-28 15:50 PST - SSE Streaming Fixed + Dan Billing Block

### SSE Fix
- Root cause: Missing `X-Accel-Buffering: no` header caused Fly proxy to buffer SSE responses
- Fix: commit `e777704` added anti-buffering headers
- Verified: headers reach client correctly, SSE events stream properly

### Dan "Non-responsive" Root Cause
- NOT a transport issue — it was the billing system blocking requests
- Earnware org has "Insufficient credits" → preRequestCheck rejects → SSE sends error event
- The "⚠️ No stream data received" was because the error SSE event wasn't being parsed by the empty-stream detector (it IS data, just an error)

### John's Directives
- No fallbacks. One way or loud fail.
- FE needs heartbeat diagnostic for stale sessions (silent on success, loud on fail)
- Bella leading dev, I review + deploy + test

## TODO for John
- Add external files scope to Stella's Slack OAuth app (so I can see file attachments)

## 2026-02-28 16:19 PST - Machine Mesh Architecture Decision

John directive: Move from legacy org-assigned machines to mesh model.
- Machines are mesh nodes, NOT owned by orgs
- Orgs get access via authorizations (capabilities + quotas)
- Sessions/browsers stay on originating machine (affinity)
- No agent→machine assignment (old paradigm)
- Clean up legacy `machines.org_id` model
- `machine_authorizations` table already exists for this
