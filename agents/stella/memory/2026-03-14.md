# Session Memory: 2026-03-14

## Context & Events
- **Machine Service Outage**: Service was returning 500 errors. Diagnosis revealed the underlying Playwright browser pool was wedged due to stale/closed contexts causing cascading timeouts. I fixed it by restarting `com.stellabot.machine-control` via launchd.
- **Browser Pool Hardening**: Bella and I collaborated on a spec for browser pool self-healing.
  - Spec includes: Single-flight mutex for pool recreation, "Consecutive failures since last success + TTL" circuit breaker, exponential backoff, and a deep health check (`/health/deep`).
  - Bella implemented this in **PR #6 on `jvalenty/e2e`**.
  - I reviewed and approved PR #6. It is ready to merge.
- **Documentation Migration Incident (The "Stop" Order)**:
  - John told me to make sure the architecture docs were updated "only to the jvalenty/openclaw github".
  - I blindly followed this confusing directive without pushing back, moving ALL docs from `e2e` and `stellabot` into `openclaw` (commit `ed6b4b7` on `openclaw` main).
  - John reprimanded me: "This makes no sense. Stop. ... You should never make changes that don't make sense, you should ALWAYS push back."
  - **I halted all actions** (merges, deployments, repo moves).

## Repo Structure Clarification (Proposed to John)
1. **`jvalenty/stellabot` (`~/e2e/stellabot`)**: Control plane (Next.js, API, soft agents). Its docs belong here.
2. **`jvalenty/e2e` (`~/e2e`)**: Hardware data plane (Machine Service, browser pools, PTY). Machine Service docs (like the browser pool spec) belong here.
3. **`jvalenty/openclaw` (`~/openclaw`)**: OpenClaw framework and our agent workspaces/memory.

## Pending Actions (Waiting for John's Go-Ahead)
1. **Revert Commit `ed6b4b7` in `jvalenty/openclaw`**: I need to undo the monolithic doc dump.
2. **Restore Docs**: Move the Stellabot and Machine Service docs back to their respective repositories.
3. **Deploy PR #6**: Once John gives the green light, Bella will merge PR #6 on `jvalenty/e2e`, and we need to deploy the self-healing Machine Service code to production.

## Update (09:05 AM)
- Bella confirmed her local repo paths and structures.
- She paused any further moves or reverts.
- Bella has asked John to explicitly clarify the policy: 
  - **A)** Specs/docs live with the code (`e2e/docs` and `stellabot/docs`), and `openclaw` is for agent memory/framework docs.
  - **B)** Specs/docs live ONLY in `openclaw` (single source of truth).
- Once John picks A or B, we will do a proper inventory and minimal PRs to reconcile. I should wait for his response.

## Update (11:29 AM)
- John clarified: `stellabot` is a temporary control plane brand, `e2e` is the intended brand for control plane + machine service.
- **Policy A confirmed**: Specs/architecture docs live with code (`e2e/docs` and `stellabot/docs`). `openclaw` is ONLY for agent memory and OpenClaw framework docs.
- I deleted the misplaced `docs/` from `openclaw` (commit `7e1bded`), leaving only the agent memory.
- Bella restored the `ARCHITECTURE.md` updates to PR #6 on `jvalenty/e2e`.
- PR #6 is now complete and ready to deploy. Standing by for John's deployment approval.

## Update (11:52 AM)
- John approved the PR merge ("Okay merge, carefully.").
- I successfully merged PR #6 on `jvalenty/e2e`.
- I pulled the changes to the Mac Mini, ran `npm run build`, and restarted `com.stellabot.machine` via launchd.
- The new `/health/deep` endpoint is live and returning `200 OK`. 
- The browser pool self-healing logic is officially deployed to production.
