# 2026-02-04

## Completed Today

### Stellabot Permission System (All 5 Phases)
- Phase 1: Schema + migrations (user_assignments, agent_capabilities, actions, agent_sessions)
- Phase 2: Permission engine (role ceilings, CRUD+A, constraints, grease calculation)
- Phase 3: UI components (CrudToggles, UserAssignmentManager, ApprovalQueue)
- Phase 4: Middleware (requirePermission, requireAgentPermission)
- Phase 5: Dashboard integration

### Neon Database Migration
- Migrated from old Neon (us-west-2) to new Neon org (us-east-1)
- John created company Neon account, invited stella@killerapps.dev
- Got neonctl CLI access with API key
- pg_dump/pg_restore migration successful
- Updated Fly.io secrets to point to new DB
- All data preserved (4 users, 9 agents, 2 orgs)

### Org Switcher Fix
- Added OrgSwitcher to AdminLayout sidebar for admins
- Fixed john@valenty.com role (now admin) and org membership (now in AMG + e2e)

### NewsDelivered Demo App
- Created new repo: github.com/stella-costa/newsdelivered
- 3-tab wizard: Brand Info → Content Brief → Templates
- Stack: React + TypeScript + Vite + Tailwind
- 6 newsletter template styles with live preview

### NewsDelivered Production Deployment
- John deployed newsdelivered to Replit with custom domain: **newsdelivered.com**
- Also available at: newsdelivered.replit.app
- Fixed SPA routing issue (direct /demo route was 404ing - needed rewrite rule for index.html)
- **CORS fix on Stellabot**: Added newsdelivered.com, www.newsdelivered.com, newsdelivered.replit.app to allowed origins
- NewsDelivered calls Stellabot API for:
  - `POST /api/scrape` - URL analysis
  - `POST /api/newsletter-share` - Save shareable configs
  - `GET /api/newsletter-share/:id` - Load shared configs

## Paused

### /pub/ Static Hosting Feature
John requested adding a public directory to Stellabot file manager:
- Files under `/pub/` served without auth at `stellabot.app/pub/*`
- Use case: host small demos, client previews, landing pages
- First demo to host: newsdelivered (now on Replit instead)

**Implementation plan:**
1. Add route to serve files from `/pub/` path without auth
2. Check files table for matching path
3. Stream file content with correct MIME type
4. Maybe add index.html auto-resolution for directories

## Notes
- Neon org: John (org-empty-cherry-36883131)
- Neon project: stellabot.app (snowy-glade-51902107)
- New DB host: ep-jolly-field-ahaab3dx.c-3.us-east-1.aws.neon.tech
- NEON_API_KEY saved in ~/.zshrc
