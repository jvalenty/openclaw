# 2026-02-23

## Morning: Bella Machine Confusion (07:15-07:55)

- John asked if Bella was alive in #sys-admins Slack
- I checked Bella's OLD OrbStack container (`orb -m bella`) — WRONG machine
- Bella has her own dedicated Mac Mini since Feb 21 (native macOS, user `bella`)
- Deleted the stale OrbStack container (John asked 3x before I did it)
- Bella was responding on Slack the whole time from her real machine
- **Lesson:** Don't guess. Check memory/notes first.

## Thunderbolt Mesh Setup (08:20-10:50)

- John plugged USB-C cable between both Mac Minis
- Bridge IPs: Stella=10.0.0.1, Bella=10.0.0.2 (set by John via System Prefs)
- SSH both directions confirmed working
- WiFi subnets are different (192.168.2.x vs 192.168.3.x) — Thunderbolt is only link
- Documented in: `memory/2026-02-23-thunderbolt-mesh.md`

## Bella Agent ID Fixed

- Bella was using MY agent ID (e9133b17). Got her own: `0d1d56bd-c388-4c43-bdca-12b5c7d38b2b`
- John manually edited her config and restarted

## Soft Agent Heartbeat Cost Fix (DEPLOYED)

- Found: 3 soft agents (Stella Soft, Dan, Jax) with heartbeats every 5 MINUTES on Sonnet = 864 calls/day = ~$31/day
- Fix 1: Changed DB cron from `*/5` to `0 *` (hourly) — 12x reduction
- Fix 2: Code change forces Haiku (mini tier) for all heartbeat calls — further 10x+ reduction
- Total: ~$31/day → ~$0.15/day (99.5% cost reduction)
- **Deployed to Fly at 11:23 AM**

## Config Template System (Bella's work)

- Bella built `~/openclaw-repo/configs/` — template.json5, agents.json, generate-config.sh
- Template uses `${VAR}` placeholders for secrets
- Template is ~70% complete, needs compaction tuning, heartbeat, contextPruning sections
- **Rule: John manually tests before any --apply**

## Machine Secrets Integration (PUSHED)

- Built `MachineSecretsTab.tsx` — new Secrets tab in machine edit page
- Predefined keys: ANTHROPIC_API_KEY, OPENAI_API_KEY, SLACK_BOT_TOKEN, SLACK_APP_TOKEN, SLACK_SIGNING_SECRET, TELEGRAM_BOT_TOKEN, OPENCLAW_GATEWAY_TOKEN, BRAVE_API_KEY, GOOGLE_API_KEY, ELEVENLABS_API_KEY
- Write-only values, rotate, delete
- Uses existing `secrets` table with `scope='machine'` (already supported)
- **Pushed but NOT deployed yet** — needs `fly deploy`

## Machine Config Tab (Bella's work, PUSHED)

- Bella built `MachineConfigTab.tsx` + server routes for config template UI
- Also pushed to Git

## Specs Written

- `docs/specs/soft-sys-admin.md` — health monitoring, auto-restart, Slack alerts
- `docs/specs/machine-config-cache.md` — local source of truth, versioned, HMAC-signed, diff-based sync

## Credential Rotation TODO

- John's mac password briefly exposed in #sys-admins (~10:44 AM)
- Full rotation list: machine passwords, SSH keys, service tokens, API keys, gateway tokens, CF tokens

## Key Rules Reinforced Today

- **DON'T GUESS** — added to top of MEMORY.md
- **NO CORE INFRA CHANGES** — investigate, document, hand off to John
- **Clear ownership** — don't jump into sibling's work (two cooks = clusterfuck)
- **Machine-scoped secrets** — not org or agent level

## Pending / Next Steps

1. **Deploy** machine secrets tab to Fly (needs John's go-ahead)
2. **John sets up secrets** via new UI for both machines
3. **Bella finishes config template** (add missing sections from my diff)
4. **OpenClaw upgrade** — Bella researched available update (2026.2.22-2), John wants to upgrade when back
5. **Config template testing** — both agents diff, John manually reviews
6. **Progressive heartbeat** — implement adaptive intervals (Phase 2 of SOP spec)
7. **Claude Code OAuth expired** — needs `/login` to re-auth

## Role Change: Architect / Developer Split (evening)

- **Stella = Architect**: Reviews all code, recommends improvements, does final push to prod
- **Bella = Developer**: Builds off task queue, does all coding
- **John = Approves production deploys**
- Prevents double-deploy, adds quality gate
- Stella still owns deploys

## Secrets Testing

- John added all Stella machine secrets via the UI (confirmed working after hard refresh)
- Next: hook config template to resolve `${VAR}` from secrets table → preview → diff → apply

## Claude Code Swarm — Cost Optimization (late night)

- John shared Elvis Sun's article about agent swarms on Max Plan: $200/mo vs our $200/day
- **Root cause of Claude Code failures**: `ANTHROPIC_API_KEY` env var (from OpenClaw) makes Claude Code prompt interactively
- **Fix**: `ANTHROPIC_API_KEY= claude -p "task"` — unset the var, use Max Plan OAuth
- **Blocker**: OAuth token expired. Needs browser login to refresh.
- **Max Plan is on Stella's machine** (stella@killerapps.dev) — Bella can SSH via Thunderbolt
- **John's idea**: 1 Claude Code sub (Stella) + 1 Codex sub (Bella) = model diversity + double capacity
- Full diagnosis: `memory/2026-02-23-claude-code-blocks.md`

**Morning TODO:**
1. John refreshes Claude Code OAuth on Stella's machine
2. Test simple swarm spawn
3. Research Codex pricing/setup for Bella's machine
4. Run a real parallel task through the system

## Platform Notes

- Stella is on OpenClaw (not Clawdbot)
- Bella's Mac Mini: Bellas-Mac-mini.local / 192.168.3.211 / 10.0.0.2 (Thunderbolt)
- Bella's Slack: U0AC82MV2RG
- Fly Clawdbot status fetch returning 530 errors every 30s (non-critical but noisy)

## Bella Ghosting on Slack (8:21 PM)
**Root cause:** Mac Mini sleep settings. Machine was going to sleep, killing all services (Slack, SSH, TB bridge).
**Fix:** `sudo pmset -a sleep 0 displaysleep 0 disksleep 0` on Bella's machine.
