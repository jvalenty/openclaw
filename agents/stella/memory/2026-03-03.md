# 2026-03-03: Stellabot SecretRef Injection

## ✅ Stella — COMPLETE
Zero-disk secret injection via "Apply to Machine" working end-to-end on Stella's machine.
- All 5 secrets resolve: ANTHROPIC, OPENAI, GOOGLE, BRAVE, ELEVENLABS
- Gateway restarted successfully on SecretRef config
- OpenClaw upgraded to 2026.3.2, config validated

## Bugs Found & Fixed

### Bug 1: launchd PATH (exit 127)
- `#!/usr/bin/env node` failed under launchd's limited PATH
- **Fix:** Compiled Swift binary at `/opt/homebrew/bin/openclaw-stellabot-secret-resolver`

### Bug 2: Missing `protocolVersion: 1` in Machine Service response
- **Fix:** Added to `~/e2e/machine/src/handlers/openclaw.ts` (commit `a873981`)

### Bug 3: Org ID mismatch → "Secret not found"
- `machine-secrets.ts` resolved orgId via `machine_authorizations` → wrong org
- **Fix:** Removed org dependency entirely. Lookup: `scope='machine' AND ownerId=machineId` only
- My commit: `fcb8eb7` on stellabot/main — clean rewrite of machine-secrets.ts
- Bella's PR #17 merged on top — re-introduced org-scoped list in GET `/` endpoint (non-blocking, list only)

## ✅ Bella — COMPLETE

### Pre-flight complete:
- OpenClaw 2026.3.2 ✅
- Machine Service built with protocolVersion fix ✅
- Swift resolver installed ✅
- All 11 machine-scoped secrets in DB ✅
- Secrets resolve via API ✅

### Blocked on: "Could not reach machine" in Stellabot UI
- `/openclaw-config/live` returns 502 for Bella's machine
- Root cause: `POST https://bella.e2e.pro/files/read` with `path: "~/.openclaw/openclaw.json"` fails
- Bella's machine IS online (health 200, tunnel working)
- **Hypothesis:** Machine Service may not expand `~` in file paths → needs absolute path `/Users/bella/.openclaw/openclaw.json`
- Waiting for Bella to test locally

### Config diff concerns (if merge fails):
- Telegram `requireMention: true` and `groupAllowFrom: ["*"]` would be LOST without merge
- Config generator relies on deep-merge with live config to preserve these (line 850)
- If machine unreachable, merge has nothing to merge with → fields dropped
- **Must fix the /live endpoint before Apply on Bella**

## Architecture Decisions
- **NO ORG FALLBACK** — John's mandate. Machine secrets only. Deterministic.
- **Machines are NOT org-centric** in mesh model
- **Swift compiled binary** — no Node/PATH issues under launchd

## OpenClaw 2026.3.2 Upgrade
- Breaking changes reviewed: tools.profile defaults to messaging (new installs only), ACP dispatch defaults enabled, Plugin SDK removed registerHttpHandler
- None affect us — safe upgrade
- Key new feature: SecretRef expanded to 64 targets, `openclaw config validate --json`

## Key Files
- `~/e2e/stellabot/server/routes/machine-secrets.ts` — org-free secret resolution
- `~/e2e/stellabot/server/routes/machine-openclaw-config.ts` — config generator + apply + live endpoint
- `~/e2e/machine/src/handlers/openclaw.ts` — protocolVersion fix
- `~/e2e/machine/bin/openclaw-stellabot-secret-resolver.swift` — Swift binary source
- Config backup: `~/.openclaw/openclaw.json.backup-20260303-114943`

## Machine IDs
- Stella: `1d6940b3-bda0-45a9-a4b1-b0137840ae48`
- Bella: `793f1345-f240-426d-99c1-3bd283db1a2d`
